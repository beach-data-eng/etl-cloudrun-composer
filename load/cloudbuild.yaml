steps:
- name: 'gcr.io/cloud-builders/docker'
  dir: load
  args: [
    'build',
    '-t',
    'us-central1-docker.pkg.dev/$PROJECT_ID/etl/load:latest',
    '.'
  ]

images:
- 'us-central1-docker.pkg.dev/$PROJECT_ID/etl/load:latest'

options:
  logging: CLOUD_LOGGING_ONLY
